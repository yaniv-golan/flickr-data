{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "flickrmail": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { 
            "type": "string",
            "description": "The unique identifier of the mail."
          },
          "from_user_id": { 
            "type": "string",
            "description": "The user ID of the sender."
          },
          "to_user_id": { 
            "type": "string",
            "description": "The user ID of the recipient."
          },
          "to_user_name": { 
            "type": "string",
            "description": "The screen name of the recipient."
          },
          "subject": { 
            "type": "string",
            "description": "The subject of the mail."
          },
          "message": { 
            "type": "string",
            "description": "The content of the mail."
          },
          "date_sent": { 
            "type": "string", 
            "format": "date-time",
            "description": "The date and time when the mail was sent."
          },
          "date_deleted": { 
            "type": "string", 
            "format": "date-time",
            "description": "The date and time when the mail was deleted."
          }
        },
        "required": ["id", "from_user_id", "to_user_id", "to_user_name", "subject", "message", "date_sent"],
        "description": "Schema for a sent Flickr mail message."
      },
      "description": "An array of sent Flickr mail messages."
    }
  },
  "required": ["flickrmail"],
  "description": "Schema for the sent Flickr mail messages."
}
