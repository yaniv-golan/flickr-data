{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "comments": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "set_id": { 
            "type": "string",
            "description": "The unique identifier of the photo set."
          },
          "comment_id": { 
            "type": "string",
            "description": "The unique identifier of the comment."
          },
          "user": { 
            "type": "string",
            "description": "The user who made the comment."
          },
          "message": { 
            "type": "string",
            "description": "The content of the comment."
          },
          "date_create": { 
            "type": "string", 
            "format": "date-time",
            "description": "The date and time when the comment was created."
          }
        },
        "required": ["set_id", "comment_id", "user", "message", "date_create"],
        "description": "Schema for a comment on a photo set."
      },
      "description": "An array of comments on the user's photo sets."
    }
  },
  "required": ["comments"],
  "description": "Schema for the comments on the user's photo sets."
}
